{
  "name": "@f/is-number",
  "description": "Check if a value is a number",
  "repository": "git://github.com/micro-js/is-number.git",
  "version": "1.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
